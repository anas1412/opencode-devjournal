{
  "name": "opencode-devjournal",
  "version": "0.1.0",
  "private": true,
  "description": "OpenCode Memory Dashboard — plugin for opencode itself",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "tsc",
    "setup": "bash install.sh"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.14.46",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.0.0"
  }
}
